# Desativa componentes que não são necessários para testes
spring.main.allow-bean-definition-overriding=true
spring.jmx.enabled=false

# Configurações de segurança simplificadas
spring.security.enabled=false